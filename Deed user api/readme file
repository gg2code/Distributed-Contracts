Type 'node index' in terminal at the location.

Start deed api in browser:

http://localhost:8080/api/Deed

Use postman to test

DataBase : deed
collection: deedusers

'mongodb'

show dbs
use deed
show collectios
db.deedusers.find().pretty();
